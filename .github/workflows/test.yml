name: Link and Run Mach-O

on: [push, workflow_dispatch]

jobs:
  link-job:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Link object file
      run: chmod +x my_app

    - name: Execute application
      run: ./my_app

